import React, {useEffect, useState} from 'react';
import {View, Text} from 'react-native';
import NoData from '../../components/noData';
const Login = () => {
  return (
    <View>
      <NoData message="login not ready" />
    </View>
  );
};

export default Login;
